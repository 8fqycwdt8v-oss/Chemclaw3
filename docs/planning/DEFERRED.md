# DEFERRED

Consciously postponed items — each with the reason it is *not now* and the trigger that would
revisit it. Default is "off-the-shelf, defer until measured".

**This is a register of what is still pending, not a log of what was decided.** A closed item is
**deleted** from it in the commit that closes it; its ADR is the record, and `git log` is the
history. Recording closure by appending a status note below a stale row is what turned this file
into nine chronological sections describing each other, three of which had gone false (D-154).

Rows are grouped by **what would have to change** — the only axis that makes a trigger checkable.
Each names an anchor in the tree, so any row can be verified with one `grep`.

## Gated on infrastructure this environment does not have

Real cluster, real Entra tenant, real registry. Nothing here is closable offline.

| Item | Why not now | Trigger to revisit |
|---|---|---|
| **Postgres RLS mirror of the graph** (KM-9) | Broad internal read access is fine for cross-project learning; a mirror adds a sync pipeline and a second source of truth against D-004 | Real, combinatorial project-level confidentiality requirements |
| **`knowledge/` as its own Git repo** | A subfolder is enough for v1 | A governance/confidentiality boundary requires the repo split |
| **The Databricks workspace** | D-2026-08-25 built both halves — a `VectorStore` over Mosaic AI Vector Search and a `Warehouse` driver over Databricks SQL — and proved them against fakes, plus manifests for the ELN and Pistachio. What is left needs the tenant itself, and specifically **three vendor facts pinned against documentation rather than a server**: the `1/(1 + d²)` score formula the adapter inverts to a cosine, the `{"column": [values]}` filter form the group scope is sent as, and `similarity_search`'s response shape (read tolerantly for this reason). The score is the one to check first — it is the only one of the three that fails *quietly*, by mis-ranking rather than by erroring | A real Databricks workspace with a Direct Vector Access index and a SQL warehouse is reachable. Then re-measure the score against known-cosine vector pairs before trusting a ranking |
| **Per-user reads from the warehouse ELN** | The warehouse connects as a service identity, so its row-level access control sees one principal and the deployment's answer to "who may see which reactions" is the view named in the binding. **Corrected 2026-08-17**: this row used to say `agent/identity/obo.py` "exists for exactly this and is dormant" and named `entra_obo_enabled` as the trigger. Neither exists — `agent/identity/` is gone and the setting with it; D-2026-08-15 deleted the On-Behalf-Of exchange as one of three controls whose only callers were their own tests, which is the exact shape of citing a dormant mechanism as evidence that a control is ready | The Databricks workspace above, plus an Entra tenant — **and** re-introducing an OBO exchange with a real caller, which is a new decision. D-046 stands as its design |
| **Push-to-registry + `helm upgrade` rollout, run** | **Written, not run** (`D-2026-08-26-a-release-is-a-descriptor-and-a-target`). The pipelines exist in all four repositories — a release is a set of image digests plus a target script, `deploy/jenkins/targets/{openshift,databricks}.sh`, and `tests/test_jenkins_delivery.py` checks every claim they make about *this tree*. What no file here can be is evidence about somebody else's infrastructure: no registry has received a push, no `helm upgrade` has touched a namespace, and no `databricks bundle` has reached a workspace. The GitHub gates are unchanged and still do the work they did | A registry, a namespace and their credentials, bound to the three Jenkins credential ids in `deploy/jenkins/README.md`. Then a `DRY_RUN=true` run to read the rendered diff, a `DRY_RUN=false` run into dev, and the environment values file that run needs (`deploy/jenkins/environments/README.md`) |
| **A chart for `Chemclaw3_ui` and the `Chemclaw3-mcp` servers** | Neither repository describes itself deployably — the fleet has seven `Containerfile`s and a per-server `networkpolicy.yaml`, the UI has a `Dockerfile` and a compose file — so `deploy/jenkins/targets/openshift.sh` deploys both with `oc set image` against a Deployment an operator created by hand. That is the honest minimum: it changes the bytes and claims nothing else. **It cannot be written from here.** Both live in other repositories, and a chart written from this one would be inventing somebody's Service, Route, probes and limits — the objection `deploy/jenkins/environments/README.md` already makes about a placeholder values file. What this repository owns is the consequence, and it now says so where an operator meets it (`D-2026-09-14-a-release-that-only-sets-an-image-says-so-where-it-bites`) | One real namespace to write against, and the repository that owns the component writing the chart — same trigger as the rollout row above, and closing that one first is what tells you what these charts have to say |
| **Live-retriever drift over the deployment's own graph** | The KM-13 gold-set (D-056) scores `GraphRetriever` over the committed fixture corpus and the F10-F2 drift job re-runs it — a deployment-consistency tripwire (`durable/eval_drift.py`). Drift over a *live, changing* graph needs labelled cases that are deployment-local, which a deployment's own graph is and this repository's is not. This repository's own gold set exists and is data rather than prose — `Probe.expects_notes`, scored as recall by `make live-probes` and validated offline by `tests/test_probe_coverage.py`, which is where how many pairs it holds is counted — and it does not close this row: a gold set over *this repository's* fixture corpus is not a deployment-local one, and it is scored on demand rather than on the drift cadence, which are the two things the first clause asks for | A deployment with a populated graph and local labelled cases exists — then score the live retriever on the drift cadence alongside the fixture tripwire |
| **Two background workers against one `background-jobs` queue** | `workers.background.replicas` is 1, and as of `D-2026-09-16-a-fingerprint-that-names-a-checkout-is-not-a-fingerprint-of-a-note` the reason is that **nobody has driven two**, not that two are known to break. Every reason previously written down is closed: the D-069 checkout lock became a Postgres advisory lock (`kg/git_writer.py::_cluster_lock`), the reindex's *retirement* half became revision-bounded (`D-2026-09-14-a-prune-needs-the-corpus-two-pods-disagree-about`), and its *re-embedding* half became a content hash — two clones of one commit now agree about every note, driven, 40 of 40 re-embedded per pass before and 0 after. What is left is an argument nobody has run: `values.yaml` claims the rest of this queue is indifferent to the worker count because the periodic jobs are Temporal Schedules under `SKIP`, the ELN cursor has one writer, retention re-checks every predicate inside its own `DELETE`, and the result outbox claims rows with `FOR UPDATE SKIP LOCKED`. Each is plausible and none has been observed with two workers polling. `strategy: Recreate` does not ride on this — its replay justification (`D-2026-09-09-a-replay-control-needs-an-archived-history-not-a-patch`) is independent of the replica count. Anchors: `deploy/helm/chemclaw/values.yaml`, `deploy/helm/chemclaw/templates/deployment-workers.yaml`, `tests/test_deploy_chart.py::test_the_singleton_worker_is_a_singleton_across_a_rollout_too` | A live Temporal broker and one database, with two workers polling one queue. What closes it is a lane rather than a reading: drive each of those four claims and assert what each one *did*, not whether it raised |
| **A live target for the results store** | `D-2026-08-25-a-cache-is-not-a-record` ships the whole path — `src/chemclaw/publish/`, the canonical schema in `schema/result-store/`, two drivers, the outbox (migration 050) and the drain — proven end to end against a local Postgres running the shipped DDL (`tests/test_publish_sql.py`). What has not happened is a deployment pointing at a real results database: `CHEMCLAW_RESULT_SINKS` is empty by default and `src/chemclaw/publish/sinks/postgres/sink.yaml` addresses a host nobody runs. Attaching one is configuration (`make sink-schema`, apply, set the variable) rather than code, which is why this is a deployment action and not a queue row — but until it happens, no number in that path has been measured against a real corpus, and `D-2026-08-26-a-route-is-not-a-shape` is why that matters more than it reads: the composite half was inert for a release and no test noticed, because every test started at a projector rather than at a hook. **Attaching one also opens a data-protection question this repository cannot answer for it**: `schema/result-store/001_core.sql` gives the external store a `calculation_publication` table with its own `actor` and `session_id` columns and an index on `actor`; `agent/leaver.py` reaches a database this system owns and cannot reach that one, and no ADR says who does. The outbox row on this side is counted and named in the erasure report as retained (`_RETAINED_IN_PAYLOAD`), so an operator sees that the receipt stays; the copy downstream is somebody else's sweep | A deployment that points `CHEMCLAW_RESULT_SINKS` at a real results database. Settle the erasure obligation with that deployment and not before — the answer depends on whose database it is |
| **Backup and restore tooling for Postgres and Temporal** | The chart dials `chemclaw-temporal-frontend.temporal.svc:7233` and namespace `chemclaw`; there is no subchart and no statement of who runs either. `docs/guides/runbook.md` § xiii ("Restore a store — and what a restore does to the audit trail", lines 1972-2025) states what this system *requires* of those stores and documents a Postgres restore procedure. What exists nowhere is tooling that performs or **verifies** a restore, and that cannot be built against a store this repository does not own. (The former separate "no backup tooling" row is folded in here; it was downstream of this one and overcounted the stores.) | Whoever owns this deployment's Postgres and Temporal being named, so a restore can be run and then verified against a store somebody controls |

## Gated on an upstream fix

Nothing here closes by writing code in this repository; each is a gap in a package this system
depends on. Re-check whenever `langchain-*` or `langgraph-*` is bumped in `pyproject.toml`.

| Item | Why not now | Trigger to revisit |
|---|---|---|
| **`mcp` 2.0.0** | **2.0.0 is released** (measured on PyPI, 2026-08-17) and has since moved to **2.1.1** (re-checked 2026-08-27) — the gap is widening, not a single release. Installed is 1.29.0 and the `mcp>=1.2.0,<2` cap in `pyproject.toml` is what holds it. This row used to read as if the release were prospective. The SDK's 2.0 renames `FastMCP` to `MCPServer` and rebuilds the low-level server around a shared dispatcher, while `connectors/server.py` patches `FastMCP._tool_manager.call_tool` **twice** (in `_bind_caller_per_tool_call` and in `_sanitize_tool_errors` — named rather than numbered, because the line numbers written here had drifted by 22 within a fortnight) — and one of those patches is the per-call caller re-binding, a security property rather than a convenience. So this is a migration with a gate to re-verify, not a version bump | A deliberate decision to pay for the migration. Then: port both patches to whatever 2.x exposes in place of `_tool_manager.call_tool`, re-run `tests/test_connector_transport.py` and `tests/test_connector_identity.py` (the two ratchets that fail if either patched behaviour stops applying), and write the ADR that lifts the cap |
| **The front door on `stream_events(version="v3")`** — built, measured, reverted | v3 retires `astream`'s tuple arity, the single largest unpromised-shape coupling in the tree, and all sixteen event-contract assertions passed unmodified (no `Chemclaw3_ui` change needed). The blocker is not maintenance cost: v3 reports token usage only at `message-finish`, so a turn abandoned mid-message books **0** tokens where the current driver books ~30 — which makes "drop the connection just before the answer" a free bypass of the token budget. A maintenance coupling is the smaller harm. Full measurement in `docs/archive/findings-2026-08.md`. Moved here from `BACKLOG.md` 2026-08-17: it closes by no code in this repository and its trigger is an upstream release, which is this section's definition — and a queue of "the next thing worth doing" should not carry a row whose own title says do not do it | Upstream emits usage per content block, or exposes the raw `(chunk, metadata)` stream alongside the content-block one. Re-check on any `langgraph` bump |
| **Prompt-cache control on the production provider** (REV-9) | Re-measured against the shipped provider stack rather than assumed to carry over: `langchain_openai` contains **zero** occurrences of `cache_control` while `langchain_anthropic` has them throughout, so the mechanism is unreachable from the `openai_compatible` path the chart ships (`grep -ro cache_control .venv/lib/python3.11/site-packages/langchain_openai --include=*.py` prints nothing and exits 1). **Only the zero is load-bearing, and only the zero is stated.** The Anthropic-side count was quoted as 74, corrected to 54, and had drifted again by the time it was audited on 2026-08-17 — three readings of a number the argument never rested on, each one work spent re-measuring a figure that decides nothing. It is deleted rather than corrected a third time, so a `langchain-anthropic` bump can no longer make this row false — the same gap the previous framework's OpenAI client had, which is why the row survived a change of framework. The fix is upstream, not a knob here. Meanwhile the prefix is not byte-stable (`tools/list` is re-fetched per turn), so one flapping connector would invalidate it anyway | Upstream exposes cache control on the OpenAI-compatible client. Before building anything, read `chemclaw_cache_read_tokens_total` against `chemclaw_input_tokens_total` — the provider may already be caching unasked (`docs/guides/runbook.md` §(viii)). Read it on a session that has *not* crossed `agent_context_token_budget`: above it, `agent/compaction.py` rewrites the front of the message list on every call — clearing tool results, then dropping the oldest conversation groups — so the cacheable prefix changes each turn by construction and a low ratio measured there says nothing about whether the provider caches |
| **A delta representation for the checkpointer's `messages` channel** | The write volume is **quadratic in a thread's length**, confirmed rather than inherited: driven through the front door with `checkpoint_retain_per_thread=0` so every version survives to be counted, a thread's `messages` blobs total **28,706 B written for a 4,278 B conversation at 5 turns, 88,675 for 7,500 at 10, 305,335 for 13,950 at 20 and 650,995 for 20,400 at 30** — an amplification of 6.7x, 11.8x, 21.9x and **31.9x**, i.e. `turns + ~2`, because `_dump_blobs` rewrites the whole channel on every superstep and a turn takes about two of them. Storage is not the problem — the shipped `checkpoint_retain_per_thread=3` keeps it linear, measured at ~2,590 B/turn — the bytes *written* are. **The mechanism that fixes it exists upstream and is beta**: `langgraph.channels.delta.DeltaChannel` stores a sentinel and replays ancestor writes, and its own docstring says the on-disk representation and the `_DeltaSnapshot`/`get_delta_channel_history` contract "may change in future releases". Adopting it means changing the representation of **live threads** on a shape upstream has not promised, which is the coupling `D-2026-08-14-the-coupling-is-the-cost-not-the-line-count` and `tests/test_upstream_surface.py` exist to keep countable. A compressing `serde` is the other candidate and is a smaller, first-party change (`agent/checkpointer._strict_serde`) that cuts the constant without changing the exponent | Either **`DeltaChannel` leaves beta** with a stable on-disk contract — then adopt it for `messages` behind a setting defaulting to off, so no existing deployment's representation changes without an operator saying so — **or** a deployment measures WAL or replication lag it attributes to this, at which point the compressing serde is the cheaper half and can be taken alone. The number to watch is the amplification above: at 100 turns a conversation writes about 100x its own size |

## Gated on a live model budget

Answer-quality questions that only a live lane can settle. Each was queued in `BACKLOG.md` as
work; none of them is, because the missing input is a measurement against a real model or a real
deployment's turns, not code. (No count here: the one that stood said three over four.)

| Item | Why not now | Trigger to revisit |
|---|---|---|
| **The judge names no claims on 88% of turns** (measurement half) | The *schema* half is closed (2026-08-17): `claims` was legally omittable because a pydantic default made it optional even under `method="json_schema"`, and it is now required with an explicit "empty means none found" contract. That is a strong causal argument and **not** a verified improvement — a provider may still return `{"claims": []}`, which is legal under the new contract and looks identical downstream | A live run re-counts non-empty `claims` over a comparable probe set. If it is still ~6/51, the cause is the judge's prompt rather than the schema |
| **Whether a `stated` quote's figure is about *this* slot** | `_quote_supports` relates a value to its quote and refuses every fabrication measured so far, and it cannot do attribution: `max_runs='24'` quoting "24 wells" passes when the chemist said 24 wells about the plate. `D-2026-09-13-a-digit-inside-a-word-is-not-a-figure-somebody-stated` delivered the count the queue row asked for first — over the 295 chemist asks in `data/evals/probes/`, **537** distinct quotable figures, of which **166 were never quantities** (SMILES ring closures, a `C18` column, the halves of a decimal) and are now refused. The remaining **371** are the attribution exposure. Closing it needs a per-slot unit vocabulary — a table that will be wrong for the first ask nobody anticipated — or a second model call inside a check that costs a regex | A deployment's own turns, so the count can be taken over what chemists actually typed rather than over asks written to grade answers. The anchor: `agent/protocol_design_tools.py::_quote_supports` |
| **An advisor tool — the one delegation shape every merged decision already permits** | The design is fully determined rather than open. `D-2026-08-25-a-summarizer-in-the-thread-and-a-condenser-behind-a-tool` settles the objection that killed the summarizer three times: its table is about **thread versus tool**, and a model call whose output returns as a `ToolMessage` has the framing envelope re-applied on the way out, is audited, authorized, dry-run refused, citable per row, withdrawable by taking one name out of the registry, and is cleared by `ClearToolUsesEdit` like any other result — a summarizer has none of those, which is why it launders an injected instruction into the model's own voice and replays it every turn. An advisor as a tool sits on the permitted side of all seven rows, and Anthropic's own advisor arrives as an `advisor_tool_result` block, which is the same answer reached independently. It is already metered and the trap that used to sit here is closed: `agent/condense.py` makes an in-tool model call whose usage reaches `agent/spend_cap.py` **because it passes no explicit `config`**, an absence `tests/test_spend_cap.py::test_no_in_tool_model_call_passes_its_own_callbacks` guards — and since `D-2026-08-29-a-guard-that-names-one-file-guards-one-file` that scan derives every module defining a registered tool and building a model, so an advisor is covered wherever it lands and the natural mistake (copying `verifier.py`'s correct `config=off_stream_metering()` into a tool body) fails naming the file and the line. `D-2026-08-16-a-second-judge-is-a-second-answer-about-the-same-answer` does not bind it: that ADR declined a *judge*, and an advisor does not grade and does not gate | A deployment whose endpoint serves a second, more capable model tier — `build_chat_model("advisor")` is the whole mechanism now that `AgentProfile.model_route` exists — plus evidence that the self-critique gap `D-2026-08-15-a-capability-that-ships-off-is-not-a-capability` named is closed by consulting rather than by thinking longer at a higher `effort`. Measure the cheaper lever first |
| **Deferring connector tool schemas behind a discovery verb** | Designed in full and deliberately unbuilt by `D-2026-08-29-a-tool-schema-nobody-calls-is-still-paid-for`, which ships as `accepted (design only)`. **This row is the one that ADR's reader was sent to and could not find**: `D-2026-09-04-tools-help-a-third-of-the-time-and-hurt-a-quarter` says "the tool-schema deferral and the `default` allow-list now have their gate — **both rows** are blocked on 'the live lane can show every probe still reaching its tool'", and only the row below it existed. The cost side has strengthened since the design: the ADR measured 33,310 schema tokens over 56 in-process tools, and `agent/context_budget.py`'s `estimate_tool_schemas` now measures ~61,100 over the `default` profile's 98 bound tools — roughly **90% of a ~67,700-token prefix**, paid on every model call, and `PREFIX_BOUND` is the input to both shipped compaction defaults. None of that is the blocker. The ADR's third condition is that the deferred arm be shown to match the bound arm **on tool selection** rather than on cost ("cheaper is already known"), and its fourth is a stop: if the eval corpus cannot separate the two arms, *leave the schemas bound*. The instrument for that now exists — `evals/live.py`'s `expected_tools_met`, published by `evals/phoenix.py` — and the before-figure is recorded: **133 of 171 expected tools reached (78%)**, 2026-09-04. What is missing is a model to run the arms against. Note the two halves are asymmetric: `wrap_model_call`'s `request.override(tools=…)` narrows only what the model is *shown*, never what `ToolNode` can run (`agent/langgraph_agent.py`), which is what makes a discovery verb able to bind a schema back — and also means nothing in `src/` exercises that upstream call today, so it is unpinned by `tests/test_upstream_surface.py` | A live lane with a real model, run as an A/B of the deferred arm against the bound arm over the probe corpus, read on `expected_tools_met` against the 78% before-figure — not on token count. If the corpus cannot separate them, the ADR's fourth condition closes this row as declined rather than deferred |
| **Narrowing the `default` profile's eleven redundant names** | Worth **-5,787 tokens (-21%)**, measured by `D-2026-08-27-eighteen-names-for-a-primitive-set`, and it is what the eighteen-tools row became once measured. The probe half is closed — seventeen probes landed, seventeen rather than eighteen because `transform_structure` was deleted rather than implemented, and the grandfathered list is gone rather than left holding an empty set — and the redundant-pair question is answered: the two bond-strength names are two capabilities, since the job's cleavage list is mandatory so it cannot answer from a SMILES at all. The ceiling was deliberately not lowered, because nothing was reduced. Two facts make this more than a one-line edit: the saving is flat in the six `enumerate_*` endpoint tools, which an offline floor cannot see at all, and the skills listing does not move, because `ensemble-workflows` stays listed after every tool it routes to is gone — so it needs the profile allow-list **and** the skill gate. Two independent surfaces raised the ceiling within two days, which argues for this rather than against it: `D-2026-08-28-a-protocol-is-prescriptive-and-a-record-is-not` added the prescriptive protocol tools, and 2026-08-29's infrastructure findings added `review_activity`, `request_external_input`, `check_pending_requests`, `review_commitments` and `assemble_evidence_pack` at 2,170 tokens after trimming, four of which are what makes the manager persona answerable at all. Every absolute here is a **lower bound**: all were measured on the basis the 2026-08-29 re-baseline corrected, which under-measured `default` by 8,126 tokens (24%) — so re-measure on the bound basis when this is worked, and read `CEILINGS["__default__"]` in `tests/test_context_floor.py` rather than any figure written down | A live lane. Part of the saving is in endpoint tools no offline floor can see, so the narrowing can neither be measured nor shown not to cost answer quality without a model behind it |

## Gated on a scale not yet reached

Each is a real optimization whose cost is currently zero. The measured current value is stated so
the trigger is checkable rather than a feeling.

| Item | Why not now | Trigger to revisit |
|---|---|---|
| **Delete the `propose_report` activity alias** | `D-2026-09-14-an-activity-name-is-a-wire-name-so-it-is-renamed-in-two-releases` renamed it to `record_report_note` and kept the old Temporal name registered, because an in-flight `DevelopmentReportWorkflow` history that has already scheduled `propose_report` resolves against whichever worker takes the task next — and one that no longer offers the name fails it with `NotFoundError` and retries for ever. Nothing can be started here: the work is a deletion that a *condition* unblocks | `background-jobs` holds no `DevelopmentReportWorkflow` execution started before the release that shipped both names. Observable, not assumed: `temporal workflow list --query 'WorkflowType = "DevelopmentReportWorkflow" AND ExecutionStatus = "Running" AND StartTime < <deploy time>'` returns nothing. Then delete the alias, its test import, and its entry in the worker's activity list |
| **Drop `note_proposed` from `evals/live.py`** | Step three of `D-2026-09-14-the-reader-lands-first-and-the-name-follows`. This service now sends `note_recorded`; this repository's own probe reader still accepts the old name so a probe run against a service one deploy behind does not score a knowledge write as absent, which would read as a retrieval regression rather than as skew | Every deployment the probes are run against sends `note_recorded` — in practice, after `Chemclaw3_ui` has completed its own step three (its `ISSUES.md` names the owner) and no older service build is live |
| **Watching for a new ELN *run*, not just a new note** | `watch_for` reports newly written notes — its docstring and `agent/subscriptions.py:161,169` still say "merged", a word left over from the PR-gate and false since `D-2026-09-05-the-gate-is-deleted-not-dormant` — and since D-2026-08-25 an ingested run is a row rather than a note — so the leading example its docstring used to give ("tell me when anyone runs a Suzuki on a chloro-pyridine") is the one thing it does not do. The naive fix reintroduces the defect the same decision removed: `durable.digest.collect_digests` matches in Python over the whole corpus, hourly, so pointing it at `reaction_records` is an O(corpus) scan on a table meant to hold an entire ELN — the exact shape that wedged the sync at ~705k. Doing it properly means matching in SQL (full-text or trigram over `body`, watermarked on `first_seen`), which is an index and a query design rather than a call-site change | A chemist asks for run-level alerting on a deployment with a real corpus. Start from the SQL side: the subscription's terms become a predicate the database evaluates, and the digest stops being a scan |
| **Sub-quadratic reaction clustering** (KM-14, half; titled *playbook* until 2026-08-28, when the callers were counted — `memory/playbook.py`, `memory/optimization.py` and `memory/observation_mining.py` all take it, so a fix scoped to playbooks fixes a third of it) | `memory/similarity.cluster_by_similarity` pairwise Tanimoto is O(n²) — simple and exact. **The stated corpus was the wrong corpus**: this clusters *ELN reactions* (it takes fingerprints built by `reaction_fingerprints(reactions)`) and the trigger is stated in reactions, but the row quoted 38, which is the number of `knowledge/` notes — inviting a reader to compare 38 notes against a 10⁴-reaction threshold. The shipped ELN corpus is **2 exports plus an ORD fixture directory**. (The *parsing* was quadratic too until 2026-08-14; that half is fixed and is not what this row waits on) **What is pending is the exponent, and only the exponent.** The constant is taken: `cluster_by_similarity` unpacks to a sparse bit matrix and takes `csr @ csr.T` into `scipy.sparse.csgraph.connected_components`, measured 4-5x on real DRFP fingerprints (348 ms → 75 ms at n=784) with byte-identical clusters at five thresholds. It is 4-5x and not the ~100x anybody would guess, because the parse had already been hoisted and what remained per pair was two `int.bit_count()`s over 2048 bits. Dense `X @ X.T` measures *slower than the Python loop* below a few hundred fingerprints — BLAS thread synchronisation costs a flat ~75 ms regardless of size — and the dense n² float64 matrix is 800 MB at this row's own trigger, which is the other reason it is sparse. None of that changes the exponent, so the pairwise O(n²) is still what this row waits on. | ~10⁴ reactions (~10⁸ comparisons per run); switch to per-reaction Postgres HNSW k-NN |
| **Postgres `pattern_bits` GIN prefilter on `molecule_fingerprints`** | `find_substructure_matches` bounds its scan to `substructure_scan_max_records` (5000) and matching runs in a worker thread under `substructure_match_timeout_seconds` (D-080). **The "warns on truncation, so both footguns are closed" clause was false and is corrected here** (D-2026-08-08-a-partial-answer-must-say-so): a 21-record store with cap 20 and the sole azide at id 900 returned `hits: []` under a verdict calling it "a genuine negative result" — the warning was a log line nobody acts on, not an answer the model reads. The scan now reports truncation in the verdict itself, and reads one row past the cap so the flag is a fact rather than the `len == cap` inference it started as. Screening with a pattern fingerprint first would raise the ceiling, but ECFP bits cannot screen substructures soundly — it needs a dedicated pattern-fingerprint column and index. That column now exists, on `corpus_molecules` (`science/labels/pattern.py`, `infra/sql/051`), and the screen is proved sound in a test; what is deferred is back-porting it to `molecule_fingerprints`, which is one migration and one rewritten function. Honest residual: the wall-clock bound frees the caller and the event loop, not the CPU (RDKit exposes no interruption hook); killing the work outright needs a subprocess. **What is pending is the screen in the database**, and the title says `pattern_bits` because an in-process screen already exists and does not close this row: `molfp/substructure_index.py` screens through `rdSubstructLibrary`'s pattern fingerprints, measured 8-25x, and still reads `all_records` per query. The `pattern_bits` column and its GIN index are what would let a multi-million-row corpus table answer without loading a slice into the worker at all | The truncation warning fires in real use, past ~10⁴ molecules — the pattern column and its GIN index are already written for the corpus table, so this is now a back-port rather than a design |
| **`within=` id-array scaling** | Retrieval eligibility ships the full eligible-id list as one SQL array parameter; fine at 10³–10⁴ notes. **Filed as a scale concern, and that is the wrong register**: with the HNSW index restored (D-2026-08-08-a-derived-index-must-record-what-derived-it) `within=` is a *post-filter* over the ef_search candidate list, so it under-returns at fixture scale today — measured 5 of 8 rows at k=8. The array-size question is still a scale question; the recall question is not, and it is **answered**: re-measured after `ANALYZE`, the note index goes short on 0 of 20 queries and the document index on 1–2 of 20, and the two knobs that address that residual now exist and default to off (D-2026-08-13-analyze-first-and-the-recall-knobs-second) | The corpus approaches ~10⁵ notes — then index type/tag/currency columns instead |
| **A durable digest of a named protocol set** | `condense_protocols` runs in the turn and refuses past `protocol_digest_max_protocols` (24) or `protocol_digest_total_max_chars` (400,000), naming the campaign synthesis as the corpus-scale route. That route exists and is fully deterministic (`OptimizationCampaignWorkflow` -> `build_optimization_notes_activity`), and after `D-2026-08-25-a-summarizer-in-the-thread-and-a-condenser-behind-a-tool` both halves share one reduce (`memory/comparison.py`). What does not exist is "digest *these specific* protocols as a job" — writing it now would be a stub for a refusal nobody has hit, and `durable.orchestrator.fan_out` is Temporal-only so it is a workflow rather than a widened tool | The turn-time refusal fires in real use — `chemclaw_protocol_digests_total` and the raised `ChemclawError` are what would show it |
| **Reagent and solvent *sets* in "changed vs previous"** | The turn-time comparison diffs temperature and time off note frontmatter exactly, and the solvent as *text* read out of the procedure. `progression._species_change` diffs canonical structures per role and is strictly better, and it needs `OrdReaction.inputs` — which the chat pod cannot reach (`D-2026-08-25-the-structure-is-discarded-at-the-note-boundary`). Diffing free-text reagent lines instead would report a change every time one procedure named a loading and its neighbour did not, which is the noise `changes_between` excludes amounts to avoid | The structured record becomes reachable at turn time — which is the same trigger as the `read_corpus` full-rescan row in `BACKLOG.md`, and one change would answer both |
| **CREST's other run types** (`--qcg` explicit-solvent cluster growth, `--msreact` fragment networks, `--entropy` as its own sampling protocol, `--mecp`, `-ttconf`) | The binary now ships (`D-2026-08-26-a-sampler-nobody-ships-is-a-refusal-with-a-manual`), so each of these is one flag and a parser away — which is exactly why they are listed rather than added. None has a caller: conformational entropy is already computed here from the ensemble the ordinary search returns, and the other three answer questions nothing in this system asks. An adopted run type with no caller is the `map_to_hpc_identity` shape a whole sweep was spent deleting | A chemist asks the question one of them answers. `--qcg` is the likeliest and has the clearest trigger: an aliphatic-amine pKa or any number where the continuum solvent is the limiting error, since explicit first-shell water is the physics ALPB is missing |
| **A better-sampled or free-energy-refined ensemble pKa** | Two refinements are available and neither is worth its cost *yet*, which is a measurement rather than a judgement. (a) **Symmetric sampling**: the neutral gets a conformer search and the anion only the microstate search's one geometry per site, so the anion's conformational entropy is missing. Split by flexibility over the 19-acid reference set, that asymmetry is worth **0.33 pKa units** of offset (rigid subset mean residual -0.16, flexible +0.17) against an overall RMSE of 1.31 — so a third CREST search per compound would buy back a quarter of the error at 50% more cost. The residual is *not* conformational: both subsets carry RMS ~1.2-1.4, and the rigid molecules (phenol, benzoic acid, 4-nitrophenol) miss as badly as the flexible ones. It is the continuum solvent's class dependence, which more sampling cannot reach. (b) **Hessian-level free energies**: one relax-plus-Hessian per member per side, `refined_ensemble`'s cost on two ensembles. Either one is also a **second reference set**, because a calibration describes the pipeline that produced it — shipping both under one slope would be one number claiming to describe two calculations | An explicit-solvent or better-parameterised continuum lands (`--qcg`, the row above), which is what would move the *class* error these two cannot. Or: the residual becomes the limiting error for a real decision, and the fit's own statistics in `D-2026-08-26-a-pka-is-a-macrostate-not-a-microstate` say whether 1.3 units is good enough before anything is paid |
| **Cross-process in-flight dedup in the calculation store** | The *in-process* half is closed — `cached_compute` single-flights concurrent misses on one key behind an `_IN_FLIGHT` future (the `_shared_probe` shape; `tests/test_store.py` drives 8 misses → 1 compute), which covers the intra-composite and intra-worker duplicates. Two misses in *different processes* still both compute (benign last-writer-wins upsert); serializing those needs cross-process locking — an advisory lock or an in-flight row — which is real machinery for a race Temporal's `job_workflow_id` reuse already collapses for identical jobs | Duplicate CREST searches on one key across two workers become a measured cost |
| **Live reattachment to a detached turn's stream** | A disconnect detaches and the turn completes; the answer reaches the client from the transcript, which `Chemclaw3_ui` polls back automatically (`D-2026-08-27-a-disconnect-is-a-detach-not-a-stop`). Streaming back *into* a detached turn — per-turn event sequence ids, a ring buffer, `GET …/turn/events?after=seq` — is machinery whose whole benefit is watching the tail live rather than reading the answer seconds later | A deployment measures that watching a detached turn's remaining progress matters — long turns whose intermediate tool results a chemist acts on before the answer |
| **Pruning unconsumed `session_events` rows** | Only consumed rows are pruned, deliberately: an undelivered completion must not be destroyed. The unconsumed population now has **three** consumers draining it (the SSE stream, the next turn's mailbox read — `D-2026-08-27-the-mailbox-reaches-the-model-not-only-the-browser` — and `GET /digests`, `api/routes/streams.py`), so what accumulates is rows for sessions nobody ever reopens: CLI launches, template runs, and the digest mailbox `digest-<oid>`, which has no `session_owners` row at all and so is reached by no session-scoped sweep. **That mailbox was a separate `BACKLOG.md` row until 2026-08-28**; it is one table, one growth mechanism and one trigger, and two registers describing it from two files is how each came to omit what the other said. The *erasure* half of the mailbox is closed rather than deferred — `leaver._ERASE` matches the channel by exact equality now | The table's unconsumed count is a measured cost; then a second, much longer cutoff scoped to sessions with no owner record, counted so the volume stays visible |
| **A bounded per-round campaign record** | Each BO round's `bo_suggestions` row snapshots the *cumulative* observation list, so N rounds store a triangular number of them: measured at 173 B/observation, a 500-round batch-1 campaign holds **22.19 MB** against the terminal write's 87.4 kB (254x), and 87.45 MB at batch 4. `retention.py` refuses `bo_campaigns` and `bo_suggestions` cascades from it, so nothing reclaims it — and the refusal is right: the snapshot is what makes an interrupted campaign resumable, so the fix is "record every Nth round", trading a bounded number of lost rounds for an N-fold reduction, not "store less per row". A second half rides with it: `record_suggestion` swallows `_TRANSIENT_WRITE_FAILURES` and returns normally, so the activity succeeds on a round that never landed and Temporal never retries it. **Carried here from `BACKLOG.md` on 2026-08-28**, where it had sat with its own `**Trigger:**` line in the file whose rows are the ones without one | The first deployment that runs durable campaigns at all — the capability map records that none ever has |
| **Batching note writes into one commit** | A note write is one commit and one push, so the fleet-wide ceiling is ~**11,000 notes/hour** and does not improve by adding pods. `D-2026-09-13-the-lock-is-not-the-bound-the-commit-is` measured where that goes: the cluster-wide advisory lock is **14.4 ms, flat**, against **298.8 ms** of git at a 10,000-note corpus — 4.8% — so the lock is not the lever and optimising it buys nothing. Batching is: **31.6 ms** per note at ten per commit, **8.5 ms** at fifty. It is not taken because a batch is a queue and a queued note is one a chemist cannot read yet, which is exactly what `D-2026-09-05-the-gate-follows-behaviour-not-knowledge` bought by deleting the PR-gate | A deployment sustains knowledge writes at a rate approaching the ceiling — read `chemclaw_notes_recorded_total` against 11,000/hour. Then the design question is the visibility delay, not the throughput |
| **Streaming the memory corpus instead of reading it whole** | Measured 2026-09-14 (`D-2026-09-14-the-memory-corpus-is-a-memory-bound-not-a-time-bound`) over a 10,000-record ORD drop directory: **396.8 MB** traced peak and 6.9 s unbounded, **144.2 MB** at a cap of 1 — so a mapped `OrdReaction` is **25.3 kB** resident and the adapter's own page of `RawEntry` is **14.4 kB** per entry. At a real deployment's ~500,000 entries that is ~20 GB in one activity's process, of which ~7.2 GB is the page: the read does not get slow, the worker is killed. `memory_corpus_max_reactions` bounds the miner's half and marks a capped pass incomplete; it **cannot** bound the adapter's page, because `OrdJsonAdapter.fetch_new_entries` accepts `limit` and ignores it deliberately (an unsorted scan would return an arbitrary subset and advance the cursor past what it skipped), so for a drop directory the page *is* the corpus and no argument `read_corpus` can pass changes that. What is left is the streaming protocol: either `ElnAdapter` gains a fetch-by-id or a bounded iterator (every source pays), or the three miners stop taking a `list` — they are whole-corpus algorithms today (DRFP fingerprinting, O(n²) Tanimoto, NetworkX components), so this is a reformulation rather than a refactor | A deployment whose corpus exceeds `memory_corpus_max_reactions`, which the WARNING names by number. It is the same trigger as the reagent/solvent-set row above: one change answers both |
| **A staleness signal for a stalled append-only feed** | `corpus_cursors` (`infra/sql/072`) records where each feed's drain stopped and nothing reads `updated_at`: `ingest/labels/cursor.py::load_corpus_cursor` selects `after` only. The module declines a lag gauge for a stated reason — a keyset position is opaque, so "how far behind" would have to be invented, unlike `sync_cursors`' datetime twin which exports `chemclaw_ingest_cursor_lag_seconds` — and what was offered instead does not hold: `ReactionCorpusWorkflow` returns **one** report aggregated over every source at the end of the whole `continue_as_new` chain, not one per pass, and builds it without `has_more`, so a feed whose source stopped exporting looks exactly like a feed with nothing new. **A record counter cannot close this, and it was briefly thought it could**: `chemclaw_ingest_records_total{source,outcome}` delivers the per-source visibility half, but a stopped feed and an idle one both produce an empty page and both book `ingested=0, rejected=0`, identically. The discriminator has to be a staleness gauge over `corpus_cursors.updated_at` — age since the last *advance*, a real number even where the keyset position is opaque — and that is now buildable where it was not when the row was written: the cursor used to be stored on every page, so `updated_at` re-stamped on every fire and measured when the feed was last *looked at* rather than when it last moved; `D-2026-08-28-a-watermark-that-is-rewritten-has-no-age` gates that write on `report.advanced`, and what is left is a reader | The first deployment that runs an `append_only:` source, since no shipped binding sets one (`D-2026-08-28-a-feed-is-a-corpus-that-does-not-stop`) |
| **An advisory audit over the `github-actions` closure** | The accepted risk `.github/dependabot.yml` names out loud (`D-2026-09-14-a-gate-for-one-ecosystem-is-not-a-gate-for-the-file`). Actions are pinned by commit, which bounds *what runs* and says nothing about whether it is vulnerable, and `make ci` reads that ecosystem nowhere. **Not built because the set is empty, and that is the argument rather than the excuse**: measured 2026-09-14 against OSV's `GitHub Actions` ecosystem, all four actions this repository uses carry zero advisories at any version, so a gate merged today would ship green forever with nothing behind it (`D-2026-08-15-a-capability-that-ships-off-is-not-a-capability`). The data source is named so the next person does not have to find it: OSV's `/v1/querybatch` takes a package named `owner/repo` in ecosystem `GitHub Actions` plus a version, and the version is the `# vX.Y.Z` comment `test_every_action_is_pinned_to_a_commit_not_a_tag` already requires beside every pin — which is a *claim* about the SHA rather than a resolution of it, and any gate built on it should say so. Anchors: `Makefile::ci`, `.github/dependabot.yml`, `tests/test_deploy_chart.py::test_every_declared_ecosystem_is_audited_or_accepted` | The first advisory that lands on an action in `.github/workflows/` |
| **Mining the edit a chemist makes to a generated protocol** | `experiment_protocol_revisions` is append-only and carries `author_kind`, so `protocols.diff.diff_designs` between an `agent` revision and the `human` revision derived from it is a *labelled correction*: the field a chemist changed, from what to what, made by the person with the most context at the moment they had it. That is the highest-quality supervision this system can collect about its own suggestions, and it is currently written and never read. **Deliberately not built, for the reason `reject_widening` was deleted**: a miner over an empty table is a mechanism whose only caller is its own test. What is owed first is a count — over the designs on disk, how many carry a human revision at all, and whether the changed paths concentrate anywhere. Anchors when it is worked: `protocols/diff.py`, `experiment_protocol_revisions.author_kind` | A deployment that has been used, so the table holds human revisions to count |
| **A distiller that turns a recurring trajectory into reusable procedure** | Six memory tiers exist and all six are *read on request* — `memory/campaign.py`, `interaction.py`, `failure.py`, `playbook.py`, `progression.py`, `observations.py`, surfaced by `recall_observations`, `find_past_jobs` and `record_failure` — and nothing in that set changes the agent's behaviour on the next turn unless a human writes a `SKILL.md`. `skills/playbook-distillation/SKILL.md` is the distillation *judgment* and the loop is manual end to end, while the 2026 work (SkillRL, SkillForge, the self-evolving surveys) is specifically about abstracting recurring trajectories into reusable procedure automatically. **The measurement this once owed is shipped; the corpus is what is missing.** `D-2026-08-27-count-the-trajectories-before-building-the-distiller` ships `make trajectory-census` (`chemclaw.cli.trajectory_census`), which prints its own verdict against the greenlight numbers, and `D-2026-09-05-a-census-that-counts-only-success-is-blind-to-half-the-signal` gave it a second arm over tools that errored across sessions, after the first arm — an identical tool-name *sequence* — reported zero on a corpus dense in repeated **failure**. Read `any_greenlit`, not `generator_greenlit`. **The tier question is settled**: knowledge is global the moment it is learned and is corrected rather than pre-approved (`D-2026-09-05-the-gate-follows-behaviour-not-knowledge`), while a **skill** is the opposite case because it is injected into the prompt with no citation trail and no agent path may write one (`agent/skill_backend.SkillsReadOnlyRefusal`) — so a distilled playbook lands through `kg/record.py` like any other note, and what needs a human is the `SKILL.md` a distiller would want to write. Two findings from the reviewed framework (WikiSkill, arXiv 2608.27454) are carried because they contradict the obvious design and cost nothing to hold: giving the *executing* agent the accumulated experience measured **worse** than not (63.7% → 60.9%) while giving it to the *proposer* was the largest ablation (+15.0pp), so experience is compiled into skills and never injected into the turn; and rejected proposals were load-bearing input, which this tree no longer retains at all — `kg/proposal.py` and its `rejected_version` went with the gate, so a distiller that wants its own rejections has to keep them itself | A deployment with sessions. Both census arms report zero on a database that has never served a user (measured 2026-08-27: 0 sessions, 0 turns, `session_turns` 0, `observations` 0), so the block is deployment history rather than effort — the day a deployment has sessions this is one command to check |
| **Review scaling and convergence over distilled skills** | Ideated 2026-09-05 and not built: how an admin avoids drowning in near-identical proposals, and how local and global skills stay convergent. Recorded here so it is not re-derived — promotion thresholds on skills (used N times **and** by ≥ 2 distinct chemists, D-161's two-threshold shape and the single most effective flood control available); duplicate suppression in the **generator** rather than in a queue, proposing an edit to the nearest existing skill unless none is close; cluster review over `cluster_by_similarity`; benefit-ranked triage over `evals/ab.py`, with the machine ordering and the human still deciding, which is why it does not re-open `D-2026-08-16`; and the convergence half — global-wins-on-conflict with the conflict surfaced, promotion retiring the local variants that fed it via `memory/supersede.py`, expiry on disuse read as a signal about the *distiller* rather than about review capacity, and `skill-validate` run on local skills at write time so form converges even where content does not. The one piece that was built — a reviewer seeing every earlier version of a note (`D-2026-09-05-a-rejection-nobody-reads-is-a-decision-taken-twice`) — was built against the PR-gate's review queue and deleted with it hours later, on the same day | The distiller above existing and producing proposals. Every part of this ideation is downstream of it, and a queue-management design with nothing in the queue is the mechanism-with-one-caller shape this register keeps rejecting |

## Gated on a capability, source or licence not in scope

Each waits on a concrete need or an external artifact, not on us.

**One capability is still gated on a model artifact this tree has not chosen.** D-089 rejects
runtime external data, and D-135 shipped the amendment — a dataset *may* be vendored into the image
at build time, checksummed and licence-labelled (`data/vendored/`, `ingest/sources/vendored_dataset.py`).
So the trigger for the tabular-FM row is "pick a licence-clean artifact and add the build step",
which is the same residual as BACKLOG's open *vendor a real third-party corpus*.

**The ML-potential row used to be counted beside it under that one blocker, and is not any more.**
`D-2026-09-19-the-condition-was-met-and-the-answer-is-still-no` closed that premise: the
build-time-vendoring mechanism D-092 asked for **exists and is proven**
(`Chemclaw3-mcp/servers/rxnpredict/scripts/fetch_models.py`, SHA-pinned under `MCP_EGRESS_ALLOW`
with the guard armed, offline at runtime), so "the weights are fetched at runtime" is no longer why
anything here is unbuilt. The row split in two on the measurement: MACE moved to the declined table
on its weights' **licence**, and ANI-2x/AIMNet2 stays here on **demand**.

**Retrosynthesis was never counted as one of these, and D-092's condition never governed it.**
Re-checked 2026-08-13 and again 2026-09-19: its blocker is not the weights and not their licence,
it is the *dependency closure* — see its row for the four mutually exclusive pins. Vendoring an
artifact into this image cannot fix a package that cannot be installed beside `rdkit`, so it takes a
different shape (its own image, reached as a `url:` connector) and does not belong under this
heading.

| Item | Why not now | Trigger to revisit |
|---|---|---|
| **ML interatomic potentials for this tier** (ANI-2x/TorchANI, AIMNet2) | **The blocker D-092 named has lifted and the new one is demand** (`D-2026-09-19-the-condition-was-met-and-the-answer-is-still-no`). Read 2026-09-19: the code and the weights are MIT on both sides (`roitberg-group/ani2x`, `isayevlab/aimnet2-*`), `torch` is already resolved at **2.13.0 in both repositories' lockfiles**, and the build-time bake pattern exists and is proven. What is missing is a caller: **zero** rows naming them in this `BACKLOG.md`, in `Chemclaw3-mcp/docs/BACKLOG.md` or in `Chemclaw3-mcp/MODULES.md`; D-092 framed them as a "fast-ab-initio surrogate" and there is no DFT left to be a surrogate for (`D-2026-08-26-semiempirical-is-the-whole-tier`); the one accuracy gap this tree has measured — aliphatic-amine pKa, Spearman **-0.17** — is the *continuum solvent*, which an MLIP inherits unchanged (`connectors/calc/compose.py::_aryl_protonation`, `docs/guides/xtb-use-cases.md`); and a second engine is a new `calc_version`, so every prediction lands `UNCALIBRATED` against a ledger keyed `(calc_type, calc_version, input_hash)` until a calibration is re-earned. The CREST/GFN2 ensemble (`Chemclaw3-mcp`'s `search_conformer_ensemble`, composed by `connectors/calc/compose.py`) covers the near-term "more realistic than one rigid conformer" need | Both halves, not either: somebody asks (a row here or in `Chemclaw3-mcp/docs/BACKLOG.md` naming the question it answers) **and** the residual error stops being solvation's. The second half is executable — the aliphatic-amine refusal is the claim, and `tests/test_calc_ensembles.py::test_an_aliphatic_amine_is_warned_about_rather_than_quietly_reported` goes **red** the day explicit-solvent work lifts it, which is the moment to re-measure the residual and reopen that ADR |
| **Retrosynthesis + reaction prediction** | **The blocker moved, and it is no longer the licence — and D-092's weight-vendoring condition never governed this row at all** (`D-2026-09-19-the-condition-was-met-and-the-answer-is-still-no`). Re-examined 2026-08-13 against the external review's claim that the licence question is "resolved, not merely narrowed", which is wrong on two counts. *(a) The licence evidence does not cover the assets that are loaded.* The review checked figshare 12334577 (MIT) — that is the **legacy Keras** package plus the `zinc_stock.hdf5` file. Current AiZynthFinder's `download_public_data` pulls its production ONNX policy/filter models from **Zenodo 7797465** and its templates from **Zenodo 7341155**, and the Zenodo API reports **CC-BY-4.0** for both. Still redistributable, so vendoring stays possible — but it carries an attribution obligation the "MIT/CC0, both clean" reading does not. *(b) The real blocker is the dependency closure.* `aizynthfinder==4.4.1` requires `numpy<2.0.0`, `networkx<3.0`, `rdkit<2024.0.0` and `pandas<3.0.0`; this repository resolves **numpy 2.4.6, networkx 3.6.1, rdkit 2026.3.5, pandas 3.0.5** (re-measured against `uv.lock` and against `aizynthfinder` 4.4.1's own metadata on 2026-09-19; pandas had moved two patch releases since 2026-08-27 and the other three had not. `python_requires` is `>=3.10,<3.13` and is **not** a fifth exclusion today, since `requires-python` here is `>=3.11` with no upper bound) — four mutual exclusions, and each of those packages is load-bearing here (rdkit across `science/`, `connectors/` (`bo`, `calc`), `ingest/eln`, `core`; numpy across `science/bo` and `connectors/calc` — **not** `cli/`, which deliberately avoids importing numpy; networkx across `kg/`, `agent/` and `memory/`; pandas in `science/bo`). So it cannot live in this tree at all, and "vendor it like xTB" is not the shape: xTB is a *binary*, not a Python dependency closure | Route planning is a real user need — and then it lands as a **separate image/repo declared here as a `url:` connector bundle** (D-2026-08-09-a-connector-we-do-not-run: hosting is a deployment fact and the URL is the whole knob), never as an in-tree dependency. Vendor its Zenodo assets at build time in *that* image under D-135's rules, carrying the CC-BY attribution |
| **The ELN run a calculation was computed for** | Not a missing column — a missing *fact*. `D-2026-09-13-a-publication-carries-the-link-the-system-already-holds` measured the three halves of the queue row it came from: the compound context already exists (`subject_member.compound_id` *is* the knowledge graph's compound note id), the note→calculation direction already exists (a note's `calc_refs` cites `calculation.calc_ref` verbatim), and the note a run produced is now carried. Which ELN entry *motivated* a calculation is named by no launcher argument, no `job_records` column and no cache row, so a column for it would be one nobody can fill — the shape two ADRs refused the same week. Recording it is a new tool argument, a new job column, and a decision about whether a model-supplied id may be stored as provenance | A result sink has a live target, so the join has a reader. Then decide where the motivating run is *stated* before deciding where it is stored |
| **A site-supplied compound identifier on a published result** | Not an identity question, which is what the queue row it came from assumed. `D-2026-09-13-a-second-identity-scheme-inherits-the-first-ones-instability` measured the argument for an InChIKey and found it false — a notation taken after standardization moves exactly when `compound_id` moves (`WYURNTSHIVDZCO` to `XBIMXRVVZSNNGI` across the solvate fix) and one taken before it splits the free base from its hydrochloride. What would survive is an id a **site** assigns, and for an ingested reaction that already rides `Component.attributes` with no core edit. The open half is whether a *published* result should carry one so another system can join on it, and it has no reader: `CHEMCLAW_RESULT_SINKS` names nothing in any shipped deployment, so the column would be dead the day it is added — precisely what `051` refuses | A result sink has a live target. Then name the joining system and the column it joins on, and add it to `schema/result-store/` beside `compound_id` rather than in place of it |
| **Tabular foundation model** (TabPFN/TabICL) | "Which experiment next" is answered by BoFire inline (`suggest_next_experiment`, D-024); a tabular FM is a different, non-critical capability (few-shot numeric prediction from a table) needing a model download and a licence check | Few-shot numeric-trend prediction over historic tables is a real need; check the model version's licence first |
| **Generic-document OCR** (typed and printed scans) | Text/CSV/TSV and PDF/PPTX/DOCX/XLSX ingress all landed (AGT-3, D-089), each read through the format's own document model; a *scanned* PDF is refused by name rather than guessed at. **The original blocker — no mature, self-hostable, permissively licensed model — has cleared** (external review 2026-08-13: DeepSeek-OCR is MIT, DeepSeek-OCR-2 Apache-2.0, and the third-party 91.09% OmniDocBench v1.5 figure belongs to OCR-2 specifically, not to both). What is *not* cleared is how the output enters the record: a mounted share's documents are indexed as **cited evidence**, not as PR-gated notes (D-2026-08-06-a-share-is-mounted-not-called), so an unreviewed OCR misread becomes citable evidence with no human between it and a chemist. **The contrast this row used to draw is gone**: it read "the opposite of the 'AI proposes, human signs off' line every other agent-generated path crosses", and after `D-2026-09-05-the-gate-follows-behaviour-not-knowledge` no agent-generated path crosses that line — knowledge is written directly and corrected. That makes the deferral *narrower*, not weaker: the three controls that replace the gate are provenance, a citation a chemist checks at the point of use, and contradiction, and an OCR misread defeats the second of the three specifically, because the citation resolves to a document that says something the scan does not. So this is a *design* deferral, not a capability one, and vendoring the weights is the D-135 build-time pattern rather than a new mechanism | A mounted share reports a `skipped_scan` count that is a material fraction of the corpus, **and** a decision is taken on how OCR output is marked where it is cited — the machine-read marker has to reach the chunk's provenance (D-160) and be visible in the citation, because there is no review step left to put it anywhere else |
| **Hand-drawn structures and spectra images** | The half of multimodal ingest that is *genuinely* unsolved, kept separate from the row above so the cleared blocker does not carry it along. Optical chemical structure recognition on **hand-drawn** structures: Enhanced DECIMER reports 99.72% valid-SMILES but only **73.25% exact-match**, and a valid-but-wrong SMILES is the worst possible failure here — it is indistinguishable from a correct one downstream, and DECIMER's own interface is human-in-the-loop for exactly that reason. Spectra-image digitization has no mature open tool at all. Neither is fixed by adopting a document OCR model | An OCR-for-structures tool reports exact-match accuracy high enough to review rather than re-draw, **or** a correction surface exists that puts a chemist between the read structure and the record (which is the same human-in-the-loop DECIMER assumes) |
| **Legacy binary Office** (`.doc`, `.xls`, `.ppt`, `.msg`) | No offline structural reader is shipped for the pre-2007 formats, so a mounted share counts them per extension and reports them rather than guessing (D-2026-08-06). Converting them means a LibreOffice subprocess: a heavy dependency with its own failure modes and its own sandboxing question, bought for a format nobody creates any more | `make share-estimate` on a real share reports an unsupported-extension count that is a material fraction of the corpus |
| **Mass balance beyond element subsumption** | `ingest/eln/validate.py` checks that no product element is absent from the inputs — a sound *necessary* condition and nothing more, so `aniline + methanol >> paracetamol`, `methane >> eicosane` and `glucose >> cholesterol` all validate. Both stronger checks need data the export does not carry, which is why this is deferred rather than queued. Heavy-atom counts need a ceiling on how often an input may repeat in a product; with no stoichiometric coefficients (a dimerization lists A once for 2 A → A–A) that ceiling is arbitrary and would reject genuine oligomerization to catch invented chemistry. Products-cannot-outweigh-inputs is sound at any stoichiometry — but measured across every shipped fixture, **no outcome records a mass**, so it would pass unconditionally while looking like a control. The reach and two of the misses are pinned by `tests/test_eln.py` so the limit stays checkable | An export carries stoichiometric coefficients, **or** outcomes carry masses. Either one makes a sound check possible; until then the reviewer is the gate |
| **Universal ingest abstraction** | `IngestHalf = ElnAdapter`, so an ingest source must expose `fetch_new_entries`/`map_to_ord` and be datetime-cursored — acceptable while every one of them is reaction-shaped. Two sources have now **sidestepped** this rather than closing it: a mounted file share (D-2026-08-06) and a bulk patent reaction corpus (D-2026-08-25-a-corpus-is-evidence-not-an-eln). Both are retrieve-only sources whose index a background job fills, the shape `vector`/`lexical` already had, so neither needed an ingest half. The second is the more telling one, because it *is* reaction-shaped and still did not fit: what it does not fit is the datetime cursor, not the mapped type (this row also said "and the PR-gate"; that half of the misfit went with the gate on 2026-09-05 and the conclusion never rested on it). That leaves the abstraction with one hypothetical caller, which CLAUDE.md's Rule of Three says to inline. D-120 also lowered the price of not generalizing: a new source is a manifest folder | A source that genuinely needs the cursored ingest half *and* does not fit `ElnAdapter` — neither the share nor the corpus does — then generalize the mapped type and the cursor contract together |
| **Durable multi-step "deep research" as a Temporal workflow** | Research is interactive Q&A — layer 1's job; `gather_evidence` plus the `deep-research` skill cover it conversationally without a durable job | A single research question needs many expensive fan-out steps that must survive a restart |
| **F10-B3 — LLM faithfulness check of drafted report sections** | The conversational verifier scores a chat turn's cited prose. The *durable* report path assembles evidence per section and renders a template — there is no free-form synthesized prose in the workflow to judge, only citations, which `verify_claims` already gates | The report workflow gains an in-workflow prose-synthesis step — then route that prose through `verify_answer` exactly as the chat runner does |
| **PMI/E-factor as a BO objective** (IDEA-3, second half) | The *tool* half shipped (`green_metrics`). An objective needs a real formulation/solvent dataset this repo does not have; inventing a problem space to host it would be a one-caller abstraction over fabricated chemistry | A real formulation case with mass data |
| **Model-based optimal design and true space filling** (BoFire's `DoEStrategy` with D/A/E/G/K/I-optimality, and `SpaceFillingCriterion`) | **The dependency reason was false and is deleted — measured, not argued.** This row said both need "cyipopt + SCIP", compiled dependencies the image would have to carry. SCIP already ships (`pyscipopt 6.2.1`, pulled transitively by `bofire[optimization]`), and cyipopt is *optional*: BoFire's `design.py` falls back to `scipy.minimize` when it is absent. Run on 2026-08-17 with cyipopt not installed, on a **constrained continuous** domain (`x1+x2 ≤ 1`) — the exact case this row calls its trigger — both `DOptimalityCriterion` and `SpaceFillingCriterion` returned a design. All six criteria import. So **no BO refusal in this file now rests on a dependency**; the sentence claiming these were the only two is gone with the reason it stated. What survives is the use-case objection alone: no story asks for an optimality criterion, 2.3 and 4.4 both ask for "a smarter reduced design" which fractional factorial plus centre points and replication answers (`docs/reference/bo-capability-map.md` W2), and space filling buys little over uniform random at n≈5 | A story asking to *design within* a constrained continuous space with a stated run budget. The dependency half of the old trigger is void — there is no cost left to wait for a deployment to pay |
| **Nonlinear and product constraints on a BO domain** | `BotorchOptimizer` does not support them, so it would take BoFire's pymoo-backed `GeneticAlgorithmOptimizer` — a worse acquisition optimizer — to serve no stated use case. The linear family covers every limit a chemist has actually stated in the story set. **The dependency half of this reason was false and is deleted**: measured, `pymoo==0.6.2` already ships transitively with `bofire[optimization]` and `GeneticAlgorithmOptimizer` imports without installing anything, so what remains is a real objection (a worse optimizer for no stated need) and not a cost | A stated nonlinear coupling between decision variables that a bound and a linear relation cannot express |
| **`NChooseKConstraint`, `InterpointEqualityConstraint` and DoE blocking** | Three shapes of one gap. "At most 3 of these 8 additives" appears in no story. The other two are batch-level — "all four wells share one temperature", "block by plate/day/operator" — and a tree-wide search finds no plate, well, day or operator concept anywhere in `src/`, so `block_feature_key` would be an argument the model fills with an invented factor | For NChooseK, a story that asks for it. For the other two, the same plate/batch entity `plate_layout` waits on |
| **A second molecular representation for BO** (`CategoricalMolecularInput`, `TanimotoGP`, BoFire's `Fingerprints`/`Fragments`/`MordredDescriptors`) | Installed and unused, and deliberately so: `science/bo/featurize.py` already featurises a molecular categorical through cached GFN2-xTB descriptors that carry `calc_refs` into the note, so a suggestion can cite the calculations that shaped its search space. A fingerprint has no equivalent provenance link, and two representations would be two competing answers to one question | A **steric** axis is needed — the stated limit of the xTB featurisation is that it is electronic only (D-096), so two ligands differing mainly in bulk look alike. Cone angle and buried volume need a 3D geometry, not a fingerprint |
| **LLM-embedding deep kernels for BO** (GOLLuM, `schwallergroup/gollum`, Apache-2.0; *Nat. Mach. Intell.*, "LLMs as uncertainty-calibrated optimizers for experimental discovery") | A third answer to the question the row above answers — how a categorical BO parameter is represented — and it is not a better fingerprint: the LLM embedding is a **deep kernel jointly trained with the GP**, the backbone's own weights optimized against the GP marginal likelihood, so the representation is *learned per campaign* rather than computed. Three costs, none of them a preference. *(a) It needs local model weights with gradients.* Its `requirements.txt` is torch + gpytorch + botorch + transformers + sentence-transformers + peft; measured, **none of torch, gpytorch or transformers is installed in this tree**, and `D-2026-09-04-a-gateway-is-the-only-provider` left nothing in `src/` that holds a model at all — every model call goes to one OpenAI-compatible endpoint, and the embedding seam (`embedding_provider=openai_compatible`) is a remote `/embeddings` route, i.e. frozen inference, which is not the method. *(b) It is the surrogate, not a primitive.* So it lands in `science/bo/engine.py`, where no surrogate class is named today and BoFire choosing the class is what makes `surrogate_fit_quality` trustworthy (M-7) — and not in `Chemclaw3-mcp`, which takes stateless primitives (`D-2026-08-16-the-physics-leaves-the-cache-stays`). *(c) Provenance, the same objection as the row above.* xTB descriptors carry `calc_refs` into the note, so a suggestion cites the calculations that shaped its search space; a learned embedding cites nothing. The data scale is against it too — campaigns here are batch 1–4 over ~10–20 observations, and a jointly trained deep kernel is the other end of that curve | Both halves, not either. A campaign whose decision space is genuinely **heterogeneous** — procedure text, or a parameter with no computable descriptor — which is the case xTB featurization (D-096) cannot serve at all rather than serves worse; **and** a measurement of our own rather than the paper's: the xTB-descriptor arm losing on `science/bo/benchmarks/reizman_suzuki.py`, which this repo already carries, at that observation budget. Then the provider posture is a **new ADR**, because a trainable backbone in-process supersedes `D-2026-09-04-a-gateway-is-the-only-provider` |
| **Feature importance over a BO surrogate** (`permutation_importance`, `shap_importance`) | *Available*, not blocked: measured, `shap` already ships with `bofire[optimization]` and the API is reachable through the surrogate BoFire itself chose. Left out because an attribution over four parameters and ten observations is a number this system cannot caveat well enough — the cross-validated fit quality in the same wave answers "should I trust this model" without inviting a per-factor reading — and because it has no second caller | A campaign large enough that a per-parameter attribution is statistically meaningful, **or** a second caller for the same machinery |
| **Blocking a low-confidence answer** | The verifier marks a low-confidence answer; it is delivered flagged, never withheld. Blocking a chat answer on a human click is a UX decision and the surface that renders the button is the frontend repo's. The in-turn form — `langgraph.types.interrupt`, which exists and works — is unused for that reason plus a harder one: resuming it needs a front-door route sending `Command(resume=…)`, and an interrupt nothing can resume is a turn that hangs. The durable hold that once carried a challenge panel's upheld objection past the end of a session went with the panel (D-2026-08-15) | A surface renders the flag as a decision point, **and** a deployment decides an unverifiable answer must be withheld rather than flagged — then add the front-door resume route and ADR the UX contract |
| **Lab automation / SiLA2 closed loop** | Requires real instrument integration; out of v1 scope | Physical/robotic execution enters scope |
| **Process flowsheet synthesis/simulation** | A separate capability area (e.g. Aspen HYSYS) | Process design, not just reaction design, is in scope |
| **Domain foundation models** | Heavy; a general LLM plus tools suffices for v1 | Task accuracy plateaus and a domain model is justified |
| **Per-bundle `log.md` changelog** (OKF, D-074) | Dropped as designed and redeferred as a redesign. The original argument — every note lands on its own PR-gate branch, so N concurrent proposals appending to one file manufacture merge conflicts — **died with the gate** (`D-2026-09-05-the-gate-is-deleted-not-dormant`): there are no branches and no proposals, `kg/git_writer.py` commits to the base branch. The conclusion survives it on its own terms and is why this stays deferred rather than reopening: a hand-appended changelog duplicates what git history already holds, and the sound form is a *generated* view (`git log` → rendered changelog). What the gate's removal actually changed is that the cost is now write contention on `_cluster_lock` rather than merge conflicts — `BACKLOG.md` carries that separately | A reviewer or auditor asks for a changelog view that does not require `git log` |
| **JS test infrastructure** | `api/static/app.js` has **no automated check at all** — no test runner, no lint config, no `package.json`, and no `node --check` anywhere in the repo (this row claimed that check as existing coverage; a repo-wide search finds the string only in this row's own text). The client is a demo shell | The web client grows beyond a demo shell |
| **A profile supplying prompt *blocks* rather than one opaque string** | The general form of `D-2026-09-14-a-profiles-prose-is-text-this-repository-wrote`. The default prompt is 31 `PromptBlock`s, each declaring `requires`/`absent_unless`, so a sentence naming a tool this deployment lacks is dropped before the model reads it. A profile's `instructions:` is one opaque string and gets none of that — the exemption `instructions_for` states is for a *site's* manifest, which this repository cannot cut into blocks, and it is right about that. What it leaves open is that a site narrowing a profile's tools has no way to narrow its own prose either: the field would have to accept a list of `{text, requires, absent_unless}` under the same ten rules. This is also the honest remainder of Wave 7's "the prompt into profile data" — the prose is already data (`data/profiles/*.yaml`); what is not is its *structure* | The first site profile that narrows tools. It has **no caller** today: every shipped profile's `instructions:` is a string, the repository-owned half is guarded by a test, and a second-domain deployment is hypothetical. Build it with that profile, not before |

## Deliberately declined

Not pending. Listed so each is not re-proposed as an oversight — the trigger column says what, if
anything, would reopen the question.

| Item | Why declined | Would reopen it |
|---|---|---|
| **MACE-OFF / MACE-MP as a potential for this tier** | **The code is MIT; the weights are not.** Read from upstream on 2026-09-19: `ACEsuit/mace`'s `LICENSE.md` is the MIT licence, while `ACEsuit/mace-off`'s README conditions the download on the ACEsuit **Academic Software Licence** — *"free to use for academic purpose, but not for commercial purposes"* — and the weights are what would be used. For a pharmaceutical deployment that disqualifies them whatever the vendoring mechanism looks like, and **no check here can catch it**: `ingest/sources/vendored_dataset.py` declares `licence: str = Field(min_length=1)`, so the field is a string and the refusal has to be a human's, which is what D-135 intends. Second and independent: `mace-torch` pins **`e3nn==0.4.4`** exactly — the same dependency-closure shape this file already refuses for AiZynthFinder, and `e3nn` is in neither repository's `uv.lock` (`D-2026-09-19-the-condition-was-met-and-the-answer-is-still-no`) | The **weights** are relicensed for commercial use — i.e. `ACEsuit/mace-off` no longer conditions the download on the Academic Software Licence, or that licence is superseded by one permitting it. Read 2026-09-19; **not executable and it cannot be made so**, because it is a fact about a document in a repository this family does not control, so what is named instead is the artifact to re-read and the date it was last read. The `e3nn` pin would still have to be answered separately, in the `url:`-connector shape the retrosynthesis row describes |
| **Literature and patent retrieval by a request-time call to somebody else's host** (TOOL-6) | **No path in this system may make an outbound call while answering** (D-089). TOOL-6 was built against PubChem's live API, reviewed and removed, and `tests/test_no_egress.py` now enforces the decision because prose in this file demonstrably did not — the earlier wording here, *blocked on choosing a source*, is what invited the build. **What was declined is the request-time call, not the corpus, and the blanket wording was wider than the decision**: D-135 shipped the amendment three sections up, so a licence-clean dataset may be vendored into the image at build time, checksummed and licence-labelled (`data/vendored/`, `ingest/sources/vendored/`) — and `BACKLOG.md`'s *`deep-research` has no index behind it* row actively wants exactly that shape, `Chemclaw3-mcp`'s proposed `litsearch` server over the Europe PMC / OpenAlex / Crossref bulk exports, built into its image and answering with no egress. Reading this row as *no literature retrieval, ever* contradicted a live queue row | A licence-clean bulk export vendored at build time and served behind a no-egress server closes the `deep-research` corpus gap without reopening anything here. A **live** API is still refused, and proposing one is a new architectural decision rather than a resumption of this one |
| **LLM extraction of what an ELN entry *learned*** | The transcription preserves what was done and loses what was learned: `_segment_steps` is a sentence split, `_classify` a substring keyword match, `hypothesis`/`failure_reason` are read only where the source already has those columns, and `OrdReaction` has **no field at all** for observations, conclusions or discussion (D-2026-08-25). A model reading the prose is the fix, and it was **costed, not hand-waved**: ~$4.8k (Haiku 4.5) to ~$9.6k (Sonnet 5) batched for 3M entries — three orders of magnitude below the human queue it replaces. Declined as a cost the owner is not willing to carry. The consequence is real and is not hidden: a chemist's insight still has to be written up by a human as a playbook or a campaign, and nothing proposes one | A budget is allocated for it. Start from the ADR's finding rather than re-deriving it — the extraction is the cheap half, the review of what it produces is the expensive one, so scope the *review* first and let that decide the batch size |
| **Second queue system** (pg-boss) | Temporal already runs, and its `background-jobs` queue covers small jobs (D-006) | Nothing |
| **LLM summarization of compacted history** | The deterministic collapse-plus-window reclaims context with no LLM call, and an untrusted summarizer is an indirect-prompt-injection risk that then *persists* in history — the injected text is re-read on every subsequent turn. (Cited D-025 until 2026-08-17; that mechanism went with the MAF framework. The live one is `agent/compaction.py` under D-2026-08-11-a-policy-nobody-can-see-is-a-policy-nobody-has) | Token-frugal collapse proves insufficient (essential older context lost) **and** a trusted summarization client exists — then add it as a further `ContextEdit` in `agent/compaction.py`, ordered after the two deterministic ones so it only ever sees what they could not reclaim |
| **Split-conformal uncertainty on a predictor** | Re-added and wired was the ask; measured, it fails three ways at once (`D-2026-08-27-an-interval-is-only-honest-where-it-was-calibrated`). The calibration set is empty and the write path cannot fill it — `predictions` and `measurements` both hold 0 rows, `calibration_enabled` defaults off, and `record_observation`'s only caller is a chemist typing into `report_measurement`. The estimator is noise at every reachable n: over 20,000 trials, ±20% at 90% confidence first arrives at **n = 59**, and at n = 20 the 5th percentile is a 25% *under*-estimate, which is the direction that reaches a chemist as an over-tight bar. And the override cannot fire, because `Estimate.uncertainty` means 1σ while a conformal half-width is a 90% interval; at matching semantics it converges on `Calibration.rmse`, which `calculator_trust` already returns with a verdict — median difference 0.032 log units at n=200. `tests/test_uncertainty.py` fails an *unwired* re-add in either half | All three, together: ≥59 logged observations for one `(calc_type, calc_version)`; a producer of measurements other than a chemist typing one in; and a stated resolution of the 1σ-vs-90% mismatch |
| **JSON payloads for structured tool results** | The ask was to replace the pydantic repr that reaches the model. Measured through a compiled graph, the premise is wrong twice (`D-2026-08-27-a-tool-result-crosses-a-boundary-and-must-say-so`): only the *in-process* half reaches the model as a repr, because FastMCP already serialises a connector result to JSON on the far side — and compact JSON is **0.4–0.8% shorter** than the repr, not larger, so context cost argues *for* it rather than against. It is declined on shape instead: a `wrap_tool_call` middleware is handed an already-stringified `ToolMessage`, so the change cannot be made in one place — it is an edit to every tool at once for half a percent, and making `Field(exclude=True)` live would arm a silent field-removal trap for a feature nothing uses. `condense_protocols`' precedent is *rendering* a boundary, which stays the rule | A tool needs a payload the model must parse rather than read, or upstream moves serialization somewhere a single seam can reach. Then render that tool's own boundary, as `condense_protocols` does, rather than converting all of them |
