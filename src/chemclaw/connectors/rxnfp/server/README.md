# `connectors/rxnfp/server` — the `rxnfp` bundle's MCP tool surface

`app.py` builds the `FastMCP` application; `tools.py` exposes the reaction-similarity search. As in
every bundle, the capability is one layer down (`science/fingerprints/rxnfp`) and this package is
the tool surface over it.
