# `connectors/rxnpredict` — a declaration this release does not run

Forward-reaction and condition prediction by ensemble. Like `connectors/chem` and `connectors/safety`, this bundle
is an `endpoint:` with no `server/`: the capability is `Chemclaw3-mcp`'s, and what ships here is the
`connector.yaml` core resolves tool names through plus the chart entry naming where to dial it.
