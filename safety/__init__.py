"""Deterministic hazard screening for agent-proposed chemistry (D-080).

Structural alerts from a committed, cited SMARTS rule table (`rules.yaml`) — advisory input to a
human review, never a safety clearance. See `safety/screen.py` for the scope and its limits.
"""
