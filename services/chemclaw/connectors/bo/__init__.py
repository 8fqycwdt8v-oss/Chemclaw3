"""The `bo` connector bundle: Bayesian optimization, inline and durable.

The reference connector-owned durable capability — its workflow, its activities and its worker all
live here, and core reaches them by workflow type name across a task queue.
"""
