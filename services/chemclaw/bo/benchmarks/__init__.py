"""Reaction-optimization benchmarks for the BO layer.

Real HTE datasets wrapped as `OptimizationProblem` + an objective, so a BoFire
campaign can be run and validated against known reaction data (plan step 1d.3).
"""
